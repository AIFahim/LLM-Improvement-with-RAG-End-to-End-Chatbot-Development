# Class 07 — only what this branch actually imports.
# (Older-class packages such as langchain==0.3.11, chromadb, pypdf and
# langchain-openai were removed: their pins conflict with current crewai,
# which made pip backtrack for a very long time.)

# Multi-Agent Systems
crewai>=1.14.0
crewai-tools[mcp]>=1.14.4
litellm>=1.55.0

# Model Context Protocol
mcp>=1.26.0

# Web Search Tool (DuckDuckGo) used by mcp_server.py
langchain-community>=0.4.0
ddgs>=9.0.0

# Web UI
streamlit>=1.28.0
